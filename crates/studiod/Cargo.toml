[package]
name = "studiod"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "video-studio 的 MCP server。唯一职能是 serve，没有子命令、不接受参数。由 Codex 自动拉起，见 docs/decisions/ADR-0002。"

[[bin]]
name = "studiod"
path = "src/main.rs"

[dependencies]
studio-mcp = { path = "../studio-mcp" }
studio-pipeline = { path = "../studio-pipeline" }
clap.workspace = true
